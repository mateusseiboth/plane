# Padrões relativos à RAIZ DA TRANSFERÊNCIA do rsync.
# Sempre sincronize a partir da raiz do repositório para que estes casem.
.git/
node_modules/
.turbo/
.react-router/
.next/
coverage/
apps/web/build/
# Artefatos do `live` construídos NO SERVIDOR (dist + node_modules resolvido
# por `pnpm deploy`). Sem estas linhas o rsync leva o build da máquina de
# desenvolvimento — ou apaga o do servidor com --delete.
apps/live/dist/
apps/live/.deploy/
# O .env da RAIZ guarda a configuração do servidor (credencial da IA, entre
# outras). Sem esta linha o rsync --delete o apaga a cada deploy e o recurso
# volta a ficar desligado sem ninguém entender por quê.
.env
apps/web/.env
apps/admin/.env
apps/space/.env
apps/api-ts/.env
apps/chat-backend/.env
apps/chat-backend/media/
apps/chat-backend/generated/
docs/manual/img/
*.log
# Worktrees dos agentes paralelos (git worktree): nunca vão para o servidor.
.claude/worktrees/
# dist dos pacotes do workspace (@plane/utils, constants, types, i18n...) é
# gerado NO SERVIDOR pelo `turbo build --filter=web^...`. Levar o da máquina de
# desenvolvimento quebrava o build do web com "X is not exported by
# packages/utils/dist" sempre que o dist local estava velho.
packages/*/dist/
# Anexos do api-ts moram no volume apimedia do servidor: a pasta local é de teste.
apps/api-ts/media/
