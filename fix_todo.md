# Fix Todo

- [ ] Validate the project-member role update flow for instance admins in `apps/api-ts/src/modules/member/index.ts` and the project access helper in `apps/api-ts/src/utils/workspace.ts`.
- [ ] Verify the technical visit create modal uses entity selection only and never submits blank UUIDs.
- [ ] Confirm urgent-issue badges render with darker chips in dark mode and open the direct issue UUID route.
- [ ] Review the issue-detail UUID route after removing the browse redirect so urgent items land on the UUID-based detail page.
- [ ] Locate custom widget creation in `apps/api-ts/src/modules/custom-widget/index.ts` and the frontend widget management surface in `apps/web/core/components/home/home-dashboard-widgets.tsx`.
