# Fix Todo

- [x] Patch issue entity updates through the Prisma `entity` relation instead of writing `entityId` directly.
- [x] Add a visit detail page so started visits can open, edit report fields, and stay editable until finalized.
- [ ] Locate custom widget creation in `apps/api-ts/src/modules/custom-widget/index.ts` and the frontend widget management surface in `apps/web/core/components/home/home-dashboard-widgets.tsx`.
