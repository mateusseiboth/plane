# Base URL of the gateway that exposes the TypeScript API (/api/v1 and /auth).
# In the local docker stack the proxy listens on http://localhost:8080.
# On a physical device, use your machine's LAN IP, e.g. http://192.168.0.10:8080
EXPO_PUBLIC_API_URL=http://localhost:8080
