FROM node:22-alpine
WORKDIR /app
COPY dist ./dist
COPY package.json ./package.json
# react-router-serve is in monorepo root node_modules; we only need the app dist
EXPOSE 3000
ENV NODE_ENV=production
CMD ["node", "dist/start.mjs"]
